// Fallback content for /legal/:slug — all four footer legal pages now have
// dedicated components (see App.jsx); this only backs the "not found" state
// in LegalPage.jsx for any other slug.
export const LEGAL_PAGES = [];
