// Fallback data source for /services/:slug — every flagship service now has
// its own bespoke page (Property Advisory, Property Management, Settlement
// Care, Leasing Care, Move-In Care), so this stays empty until a new service
// is added that doesn't warrant a custom layout yet. ServiceDetailPage.jsx
// renders a "service not found" state when a slug has no match here.
export const SERVICE_DETAILS = [];
